#install
install_chromo <- function() {
  if(!require(remotes)) install.packages("remotes")
  require(remotes)
  if(!require(chromote)) remotes::install_github("rstudio/chromote")
  require(chromote)
}
